// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/ubuntu
{
	"name": "RZV2H DRP-AI Extension Pack",
	"dockerComposeFile": ["../../docker-compose.yml"],
  	"service": "Extension",
  	"shutdownAction": "none",
	"forwardPorts": [4000,4000],

	// Change where the VSCode is mounted in the Container
	// "workspaceMount": "source=${localWorkspaceFolder}/Extension,target=/home/workspace,type=bind",
	"workspaceFolder": "/workspace/Extension"
}
